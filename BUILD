# BUILD
package(default_visibility = ["//visibility:public"])

# The WASM platform definition
platform(
    name = "wasm32_wasi",
    constraint_values = [
        "@platforms//os:wasi",
        "@platforms//cpu:wasm32",
    ],
)

filegroup(name = "empty")
